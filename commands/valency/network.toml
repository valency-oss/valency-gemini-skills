description = "Map a researcher's collaboration network and divergence from top collaborators."
prompt = """
Activate the `network` skill and map the collaboration network for: {{args}}

Follow the skill's tool chain exactly, including the divergence analysis. Use the Valency MCP tools loaded by this extension.
"""
