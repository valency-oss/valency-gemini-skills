description = "Find recent, thematically relevant researchers outside an author's existing coauthor network."
prompt = """
Activate the `fresh-collaborators` skill for: {{args}}

Follow the skill's tool chain exactly: build the exclusion set from the top-100 coauthors, identify 2–4 current themes, run date-restricted semantic searches, filter against the exclusion set, and rank fresh-face candidates. Use the Valency MCP tools loaded by this extension.
"""
